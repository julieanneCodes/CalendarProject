<form method="post" action="{{ path('view_config_delete', {'id': view_config.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ view_config.id) }}">
    <button class="btn">Delete</button>
</form>
