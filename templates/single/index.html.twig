{% extends 'base.html.twig' %}
{% block stylesheets %}
    {{ encore_entry_link_tags('single') }}
{% endblock %}
{% block title %}Bethink{% endblock %}
{% block body %}
    <user-header usId="{{ userId }}"></user-header>
    {{ include('search/searcher.html.twig') }}
    <a href="{{ path('calendar_new') }}">Add new event</a>
    <a href="{{ path(route) }}">{{ routeName }}</a>
    <calendar-component id="content" data="{{ data }}" titlePadding="{{ titlePadding }}" calendarPadding="{{ padding }}"></calendar-component>
{% endblock %}