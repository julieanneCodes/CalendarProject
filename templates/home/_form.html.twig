<div class="emailForm">
    {{ form_start(form) }}
        {{ form_widget(form) }}
            <div class="btnWrap">
                <button class="btn">Sign Up</button>
            </div>
    {{ form_end(form) }}
</div>