{% extends 'base.html.twig' %}
{% block title %}Bethink | Calendar & Tasks{% endblock %}
{% block body %}
    {{ include('search/searcher.html.twig') }}
    <calendar-view id="content" calendarData="{{ data }}" tasksData="{{ taskData }}" userId ="{{ userId }}"></calendar-view>
{% endblock %}
